If you take the provided numbers, convert them to binary and stack them (with a set of three being in the same row), then you'll see that the 1's actually spell out "OPY", which is the answer. To see this in action:<br />
<br />
<img src="../../numberssolve.png" />