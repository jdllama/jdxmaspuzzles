<!DOCTYPE html>
<html>
<head>
<title>
<%=title;%>
</title>
<style>
.guess {
  display: none;
}

#showguesses:checked~ul>li.guess {
  display: block;
}
</style>
</head>
<body>
<% if (show_solutions == 1) { %>
<a href="/ShowMeTheSolution/">Solution</a>
<% } %>
    <fieldset>
        <legend>Username</legend>
        <form method="POST" action="/setusername"><input autocomplete="off" type="text" id="username" name="username" value="<%=username;%>" /><input value="Update Username" type="submit" /></form>
      </fieldset>
  <h2><%=title;%></h2>
<%- include(partial) %>
<!-- Everything below this is NOT puzzle related. I PROMISE. As such, here's a whole bunch of blank lines because hey, you might not want spoilers, right? <3 -->







































































































































































































  
<br />
<br />
<% if (isRight == "true") { %>
  Your guess was correct!
<% } %>
<% if (isRight == "false") { %>
  Your guess was incorrect.
<% } %>
<hr style="width:100%">
Remember, all puzzle answers are only letters, no symbols or numbers.<br />
<br />
<form id="myForm" method="POST">
  <%=username;%> has a guess of <input type="text" placeholder="Puzzle Answer" name="answer" id="answer" />
  <input type="submit" text="Submit">
</form>
<br />
<br />
<h2>Guesses</h2>
Click here to view <%=guesses.length;%> guesses: <input type="checkbox" id="showguesses" />
<ul>
<% for(var i = 0;i<guesses.length;i++) {%>
<li class="guess">
  <% if(guesses[i].didsolve == 1) { %>
    <b>
  <%}%>
  <%=guesses[i].guess;%> guessed by <%=guesses[i].player;%>
  <% if(guesses[i].didsolve == 1) { %>
    </b>
  <%}%>
</li>
<%}%>
</ul>
<br />
<br />
<%- include("../pages/footer.ejs") %>
</body>
</html>
